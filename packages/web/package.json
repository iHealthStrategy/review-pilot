{
  "name": "@reviewpilot/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node --check scripts/build.mjs",
    "test": "node --test test/",
    "clean": "rm -rf dist"
  }
}
